@model MessengerClientDB.Models.ViewModels.UserRolesVM

@{
    ViewBag.Title = "Delete User";
}

<h2>Delete User</h2>
<div>
    <h5>Deletes the user & user roles from both the client & service databases. The user's sent & received messages are deleted from the client database, & the user's received messages are deleted from the service database.</h5>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Username)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Username)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.MyRoles)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.MyRoles)
        </dd>
    </dl>
</div>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Delete" class="btn btn-default" />
            </div>
        </div>
    </div>

}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>


